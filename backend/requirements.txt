# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
pydantic-settings==2.5.0
python-dotenv==1.0.1
python-multipart==0.0.9

# Database
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.9
alembic==1.13.0

# Queue — SQS broker (GPU autoscaling watches SQS depth), Redis result backend.
celery[sqs]==5.4.0
redis==5.1.0
pycurl==7.45.3  # kombu's SQS transport needs it for async polling

# Storage
boto3==1.35.0
aioboto3==13.0.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── AI / ML ────────────────────────────────────────────
# See ARCHITECTURE.md §2. Every model below is commercial-safe; the License
# enum in providers/base.py is the enforcement point, not this file.
httpx==0.27.0
torch==2.4.0
transformers==4.44.0
numpy==1.26.4
Pillow==10.4.0

# Image (FLUX.1-schnell) + video (Wan 2.1) + music (Stable Audio Open).
# Wan (WanPipeline, AutoencoderKLWan) landed in diffusers ~March 2025; 0.31 does
# NOT have it and the import would fail. Floor-pinned above that, capped so a
# future breaking release can't silently upgrade under us.
diffusers>=0.33.0,<0.40.0
accelerate>=0.34.2
sentencepiece==0.2.0
protobuf==5.28.0

# Voice — Kokoro (English, Phase 0-1).
# Phase 2 adds IndicF5 for Telugu/Hindi; Kokoro has no Telugu at all.
kokoro==0.3.4
soundfile==0.12.1

# Alignment — drives subtitle timing off the generated audio.
faster-whisper==1.0.3

# Video processing
ffmpeg-python==0.2.0

# Utilities
jinja2==3.1.4
orjson==3.10.0
aiofiles==24.1.0
tenacity==9.0.0
structlog==24.4.0
uuid7==0.1.0

# ── Test ───────────────────────────────────────────────
# The pure-logic suite (timing, routing, subtitles) runs with no GPU and no
# model weights -- only these two are needed for it.
pytest==8.3.3
pytest-asyncio==0.24.0
